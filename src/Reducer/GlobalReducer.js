import actions from './actions';

const globalReducer = (state, action) => {
  switch(action.type){
    case actions.PARSE_DATA:

    default:
      return state
  }
}

export {globalReducer, actions};
